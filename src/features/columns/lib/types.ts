export type Column = {
  key: string;
  name: string;
  dataType: string;
  id: string;
  variants: string[];
};

export type OriginColumn = {
  name: string;
  dataType: string;
  id: string;
  variants: string[];
};
